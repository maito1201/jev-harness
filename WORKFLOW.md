# Evidence-controlled workflow

TAKT の外部工程制御・役割・許可操作・明示的な遷移を参考に、Jev の意味判定と JavaScript の状態遷移を分離した。TAKT 本体への依存や別エージェントの起動はない。

`design → premises → implementation → evaluation → execution → results → complete`

- 設計: 目的・検収・検証計画を審査し、評価ファイルの SHA-256 を固定する。
- 前提確認: 実際の検査結果で前提を確認する。
- 実装: 実装変更と小規模検証を許可する。
- 評価方法の検査: 固定目的を測り、誤った実装を落とせる独立検査を要求する。同一コマンドの再利用は拒否する。
- 実行: 前工程の実証とコード版が一致する場合だけ本実行を許可する。
- 結果確認: 実行結果を固定基準と照合する。プロセス終了だけでは完了しない。
- 完了: 工程完了に加え、報告の主張と観測結果を照合する。

許可表と遷移は hooks/workflow.json、実装は hooks/workflow.mjs と hooks/enforce.mjs。Jev は操作・証拠・失敗原因を分類するが、次の工程を自由に指定できない。

失敗は実装なら実装工程、評価方法なら設計、前提なら前提確認へ戻す。観測不足は現在工程を維持し、原因不明は停止する。同じコード・基準・コマンド・原因の失敗が3回続くと停止する。成功証拠は失効する。修正・再設計後に必要工程を再実行する。

評価ファイルへの変更要求は設計以外では一旦拒否し、設計へ戻す。hook 外の変更も次のイベントで指紋を照合する。実装変更は小規模検証からやり直す。内部継続通知では元の要求と承認を保持する。

## 検証

`node --test`: hook を子プロセスとして起動する回帰テストを含む40件。

`node eval/workflow-live.mjs <output.json>`: 実際の Jev と hook 子プロセス、実際の4入力 fixture を使う。前提確認から最終報告までの正常経路、先行実行の拒否、評価基準変更による失効を確認する。会話・研究データは使わない。旧コマンド eval/phase-gates-live.mjs も同じ検証を起動する。

## 適用範囲と制約

- ホストが hook を実行して拒否を尊重することが必要。CLI の hooks/list は登録・信頼状態の確認であり、既存デスクトップタスクでの実行証明ではない。更新後は新しいタスクで読み込む。
- Stop hook は応答後の継続・差し戻しであり、すでに表示された文章を隠さない。
- 工程の役割は操作制限であり、別プロセスの人格分離や OS sandbox ではない。
- Jev の意味分類は誤る可能性がある。工程を守ったことだけでは科学的な正しさや機械学習能力を保証しない。
- 評価指紋はルートの test/tests/eval/evaluation/.jev と認識対象の contract/criteria/acceptance/evaluation ファイルを対象とする。任意の外部データや登録外ディレクトリは自動固定しない。
- 依存ソース取得は静的な相対 import と literal readFileSync 等に限定する。動的依存の完全解析ではない。
- 非同期の未終了処理は合格証拠にしない。長時間ジョブの再接続による正常遷移は今回の検証対象外。
- 人工 fixture での成功は研究データ上の成果や現在のデスクトップホストでの差し戻し成功を示さない。
